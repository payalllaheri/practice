import React from "react";
import TypewriterEffect from "./Abch";

const Heading = ({ title }) => {
  return (
    <div style={{ fontSize: "68px", marginBottom: "100px" }}>
   {title}
    </div>
  );
};

export default Heading;
